"use strict";
// import QRCode from 'qrcode';
// export const generateQrCodeUrl = async (googleMapsLink: string): Promise<string> => {
//   return new Promise((resolve, reject) => {
//     QRCode.toDataURL(googleMapsLink, async (err, url) => {
//       if (err) reject(err);
//       resolve(url);
//     });
//   });
// }
